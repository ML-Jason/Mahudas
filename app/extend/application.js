const utils = require('./utils');
// const logger = require('./logger');

module.exports = {
  // 擴充app，例如：
  // ext() {
  //    console.log('this is an app extend');
  // },
  // 就可以透過app.ext()呼叫

  utils,
  // logger,
};
